export { initBridge, getProxy, registerEventReducer } from "@/bridge/channel";
export { useInvoke, useEvent, useBridge } from "@/bridge/hooks";
export type { BridgeProxy, EventListener } from "@/bridge/types";
